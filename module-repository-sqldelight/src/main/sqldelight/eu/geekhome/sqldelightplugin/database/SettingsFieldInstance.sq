CREATE TABLE IF NOT EXISTS settingsFieldInstance (
  clazz TEXT NOT NULL,
  name TEXT NOT NULL,
  value TEXT,
  PRIMARY KEY(name, clazz)
);

selectAll:
SELECT *
FROM settingsFieldInstance;

insertOrReplace:
INSERT OR REPLACE INTO settingsFieldInstance(clazz, name, value)
VALUES (?, ?, ?);